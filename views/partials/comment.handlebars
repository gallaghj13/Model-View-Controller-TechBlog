<div>
    {{#each comments as |comment|}}
    <section>
        <div>
            <h4>Posted By: {{user.username}} on {{format_date date_created}}</h4>
        </div>
        <div>
           <p>{{comment.comment}}</p>
        </div>
    </section>
</div>